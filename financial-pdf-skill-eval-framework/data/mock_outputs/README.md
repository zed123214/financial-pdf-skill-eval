# Mock outputs

Drop offline `result.json` / `parsed.md` / `pages_detail.json` triples here to
feed the Skill's `official_output_mock` backend via `--mock-dir`.

Each subdirectory should match a `case_id` from your YAML cases.
